VS + Flutter

API:
-json_serializable (pacchetto) -> serializzare tutto i progressi come json
-path_provider -> usato per salvare il json come file temporaneo
-share_plus -> usato per convidivere il file temporaneo e salvarlo idealmente su google drive
-file_picker -> usato per inglobare un file esterno nell'app (quindi ricaricare i progressi nell app)